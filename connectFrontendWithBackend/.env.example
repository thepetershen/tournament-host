# Database Configuration
DB_URL=jdbc:postgresql://localhost:5432/tournament_host
DB_USERNAME=tournament_manager
DB_PASSWORD=your_database_password_here

# JWT Configuration
JWT_SECRET_KEY=your_long_random_secret_key_here_at_least_64_characters_long
JWT_EXPIRATION_TIME=86400000

# Server Configuration (optional)
SERVER_PORT=8080

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:5173

# Instructions:
# 1. Copy this file to .env (which is gitignored)
# 2. Replace all placeholder values with real secrets
# 3. NEVER commit .env to git
# 4. Share .env.example with your team (without real secrets)
