# ==============================================
# Tournament Host Environment Configuration
# ==============================================
# Copy this file to .env and fill in your values
# NEVER commit .env to git!

# ---------------------------------------------
# Database Configuration
# ---------------------------------------------
DB_NAME=tournament_host
DB_USER=tournament_manager
DB_PASSWORD=your_secure_database_password_here_change_this
DB_PORT=5432

# ---------------------------------------------
# JWT Security Configuration
# ---------------------------------------------
# Generate a secure random key (at least 64 characters):
# You can use: openssl rand -base64 64
JWT_SECRET_KEY=your_long_random_secret_key_here_minimum_64_characters_CHANGE_THIS_VALUE
JWT_EXPIRATION_TIME=86400000

# ---------------------------------------------
# Email Configuration (SendGrid)
# ---------------------------------------------
# Get your API key from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.your_sendgrid_api_key_here

# ---------------------------------------------
# Application URLs
# ---------------------------------------------
# Backend API port
BACKEND_PORT=8080

# Frontend port
FRONTEND_PORT=80

# Frontend URL (for CORS and email links)
FRONTEND_URL=http://localhost:5173

# API base URL for frontend (in production, use your domain)
VITE_API_BASE_URL=http://localhost:8080

# ---------------------------------------------
# Spring Profile (optional)
# ---------------------------------------------
# Options: dev, prod
SPRING_PROFILES_ACTIVE=prod

# ==============================================
# PRODUCTION DEPLOYMENT NOTES
# ==============================================
# For production, change these values:
# 1. Use strong, unique passwords for DB_PASSWORD
# 2. Generate new JWT_SECRET_KEY using: openssl rand -base64 64
# 3. Set FRONTEND_URL to your actual domain (e.g., https://yourdomain.com)
# 4. Set VITE_API_BASE_URL to your backend URL (e.g., https://api.yourdomain.com)
# 5. Ensure all ports don't conflict with other services
